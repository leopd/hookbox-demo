To use this, make sure you have cherrypy and hookbox:
    sudo easy_install cherrypy
    sudo easy_install hookbox
And that the rubber_stamp app is running
    python ../ows/rubber_stamp.py
And that hookbox is running;
    hookbox --cbport=8080 -a adminpassword
Watch hookbox from
    http://localhost:8001/admin/
Then run the script by opening
    chat.html


